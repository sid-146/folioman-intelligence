[project]
name = "folioman-intelligence"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sudhawna-pc", email = "sudhanwa.kaveeshwar@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.7.13",
    "httpx>=0.28.1",
    "langchain-openai>=1.6.2",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "pytest-cov>=7.1.0",
]

[project.scripts]
folioman-intelligence = "folioman_intelligence:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "respx>=0.23.1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

